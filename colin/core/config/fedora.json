{
  "labels": {
    "required": [
      "maintainer",
      "name"
    ],
    "optional": []
  },
  "dockerfile": {
    "required": ["from_tag", "maintainer_deprecated"],
    "optional": ["layered_run"]
  }
}