/test/build:
    /smoke:
      execute:
        script:
        - colin --version
        - colin --help
        framework: shell
    /list-rulesets:
      execute:
        script:
        - sh -c "cd / && colin list-rulesets"
        framework: shell
    /list-checks:
      execute:
        script:
        - sh -c "cd / && colin list-checks"
        framework: shell
